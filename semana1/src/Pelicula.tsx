
export const Pelicula = ({titulo, director}: {titulo: string, director: string}) => {
    return (
        <div>
            <h1> El titulo de la pelicula es: {titulo}</h1>
            <h1> El director de la pelicula es: {director}</h1>
        </div>
    )
}

